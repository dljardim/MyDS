import SwiftUI

enum LightColors: ColorToken {
    static let primary = Color(hex: "#007AFF")
    static let background = Color.white
    static let textPrimary = Color.black
    static let textSecondary = Color.gray
    static let accent = Color.pink
}
