import SwiftUI

enum DarkColors: ColorToken {
    static let primary = Color(hex: "#0A84FF")
    static let background = Color.black
    static let textPrimary = Color.white
    static let textSecondary = Color.gray
    static let accent = Color.purple
}
